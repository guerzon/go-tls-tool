package cmd

import "github.com/spf13/cobra"

func init() {
	certCmd.AddCommand(sign)
}

var sign = &cobra.Command{
	Use:   "sign",
	Short: "Sign a CSR and generate a certificate.",
	Long:  `Sign a CSR and generate a certificate.`,
	Run: func(cmd *cobra.Command, args []string) {

	},
}
